<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:label="${extName}">
        <meta-data android:name="newshub.extension" android:value="true" />
        <meta-data android:name="newshub.extension.source_class" android:value="${extClass}" />
        <meta-data android:name="newshub.extension.name" android:value="${extName}" />
        <meta-data android:name="newshub.extension.source_id" android:value="tw.kevinzhang.komica-sora" />
        <meta-data android:name="newshub.extension.source_name" android:value="Sora Komica" />
        <meta-data android:name="newshub.extension.source_lang" android:value="zh-TW" />
        <meta-data android:name="newshub.extension.source_base_url" android:value="https://komica1.org" />
    </application>
</manifest>
