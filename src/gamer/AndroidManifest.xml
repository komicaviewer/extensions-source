<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <meta-data
            android:name="newshub.extension"
            android:value="true" />
        <meta-data
            android:name="newshub.extension.source_class"
            android:value="${extClass}" />
        <meta-data
            android:name="newshub.extension.name"
            android:value="${extName}" />
    </application>
</manifest>
